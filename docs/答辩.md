# 答辩

- Source: `答辩.pptx`
- Total slides: 16

## Slide 1

COURSE DEFENSE · JUNE 2026

ReasonGenPilot

A Training-Free Agent for

Image Generation & Hypothetical Editing

ROUTES

gen · edit · hybrid

三条 pipeline 共享同一 result schema

Team of 4 · ReasonGenPilot · Course Defense 2026

01/16

### Speaker Notes

各位老师同学下午好。我们今天答辩的项目叫 ReasonGenPilot，一个完全无训练的图像生成与假设性编辑 Agent 系统。三条互补通路 gen edit hybrid 共享一个 router 和一个统一返回 schema。团队四人，分支 testWork3，十五页讲完，约十二分钟，欢迎随时提问。

## Slide 2

Background & Goal

PROBLEM

02/16

T2I 的两类失败 — 我们的一个目标

Two failure modes of off-the-shelf T2I models — one goal.

MODE A

MODE B

Descriptive misalignment

Hypothetical instructions

复杂 prompt 下：数错对象、漏色、空间

反事实指令需要常识推理才能落地，

关系错位 — 描述与图像对不齐。

T2I 模型本身完全不具备此能力。

EXAMPLE

EXAMPLE

“six steamed buns on a round plate”

“what if the ice cubes melted?”

RESULT

OUR GOAL

VQA 1.0

Zero training.

MLLM Agent + 公开 API，

4 个端到端 case 全部满分

不微调、不自建数据，覆盖两类失败。

ReasonGenPilot · Course Defense 2026

02/16

### Speaker Notes

我们要解决的是当前 T2I 模型的两类典型失败。第一类是描述性失配，复杂 prompt 下数错对象、漏色、空间关系错位。第二类是假设性指令，需要常识推理才能落地，T2I 完全不懂。我们的目标用一句话总结：零训练，只用 MLLM Agent 加公开 API 解决这两类问题，不动一个权重，最终四个端到端 case 全部 VQA 等于一点零。

## Slide 3

System Architecture

OVERVIEW

03/16

Router → 三通路 → 统一返回 schema

Router fans the user input out to gen / edit / hybrid; all three converge on a unified result schema.

UNIFIED SCHEMA

USER INPUT

prompt + optional image

final_image

route

reasoning_chain

vqa_result · metadata

MLLM

Router

prompts/router_system.txt

gen

edit

hybrid

text → image

局部指令编辑

整图重绘

prompt 优化循环

保留原图像素

原图仅用于推理

ReasonGenPilot · Course Defense 2026

03/16

### Speaker Notes

这是系统总览。用户输入进入 Router；Router 通过 MLLM 自动分类，把请求扇出到三条通路之一。三条 pipeline 共享同一个返回 schema —— 最终图像、推理链、VQA 结果、元数据。三通路互补，整个系统对外只暴露一个入口。

## Slide 4

Three Complementary Routes

PIPELINES

04/16

Routed by input shape · 改完后还需原图像素？

Three routes share the unified `--prompt` field (gen: description; edit / hybrid: counterfactual instruction).

gen

edit

hybrid

RECOMMENDED · 整图重绘

INPUT

INPUT

INPUT

仅文本 prompt

图 + 反事实指令

图 + 反事实指令（整图变换）

GOAL

GOAL

GOAL

优化 prompt，使得 T2I 输出

局部指令编辑，

T2I 从零重绘，

与描述对齐。

尽量保留原图像素。

原图仅供 Reason 推理。

LOOP

LOOP

LOOP

decompose → T2I baseline

Reason → Qwen-Image edit

Reason(hybrid) → scene_prompt

→ K candidates → VQA select

→ VQA select → refine loop

→ 7-layer fix → gen → VQA

EXAMPLE

EXAMPLE

EXAMPLE

“A grass field of red poppies

“What if the ice cubes

“What if this bouquet split

beside a wooden windmill.”

melted?”

into two?”

改完之后还需要原图像素吗？ Yes → edit · No → hybrid · No image → gen

ROUTING RULE

ReasonGenPilot · Course Defense 2026

04/16

### Speaker Notes

这一页摊开三条通路的输入输出对比。判别准则只有一条：改完之后还需要原图像素吗？需要走 edit，在原图上做局部编辑；不需要走 hybrid，整图从零重画。无图直接走 gen。注意我们的统一字段是单个 dash dash prompt，gen 当描述读，edit 和 hybrid 当反事实指令读。

## Slide 5

Member 1 — gen Route

FOUNDATION · gen

05/16

基座 · 共用 schema · GenPilot 风格 prompt 优化

MLLM / T2I clients · shared dataclasses · 5-step optimization loop.

MODULES

OPTIMIZATION LOOP

Decompose

1

api_client.py

把 prompt 拆为可校验的约束

OpenAI-兼容 MLLM — 文本 + 视觉，base64 + resize

Baseline

2

用 T2I 出 baseline 图

t2i_client.py

dry_run / dashscope / siliconflow / comfyui

K candidates

3

生成 K 个候选 prompt，逐个 T2I

gen_pipeline.py

VQA select

4

decompose → baseline → candidates → VQA select

MLLM VQA 打分，保留最好的

Update + loop

5

schemas.py

用最优 prompt 替换 current — 循环 N 轮

共享 dataclass — 三通路全部继承

CASE · POPPY + DAISY + WINDMILL

VQA SCORE

0.8 → 1.0 → 1.0

baseline 漏 “yellow daisies”，2 轮迭代后修复 · 真实 API（DashScope Qwen-Image）

ReasonGenPilot · Course Defense 2026

05/16

### Speaker Notes

成员一负责基座加 gen 通路：api_client、t2i_client、gen_pipeline、schemas。核心是 GenPilot 风格的五步优化循环：拆约束、出 baseline、生成 K 个候选 prompt、MLLM VQA 打分、用最优 prompt 替换并循环。Poppy Daisy Windmill 这个 case，VQA 从 baseline 零点八，经两轮迭代提到一点零。

## Slide 6

Member 2 — edit Route

REASON · EDIT · VQA

06/16

四类推理 + Qwen-Image 指令编辑 + VQA 闭环

Reasoning types aligned with Reason50K · 5-step pipeline · refine until threshold.

REASONING TYPES

PHYSICAL

TEMPORAL

CAUSAL

STORY

物理变化

时间推进

因果链条

叙事推理

冰块融化、跷跷板倾斜

白天变深夜、衰老

蜜蜂落花、跷跷板

隐藏纹理浮现

PIPELINE

1

2

3

4

5

Reason

Finalize

Edit

VQA

Refine

Reason Agent

Qwen-Image 指令编辑

K 候选 VQA 评分

finalize_edit_prompt

visual_cues

注入物理约束

无 mask

checklist 清单式打分

不达标

+ physics_implications

image + text

保留最好的

+ 保留对象列表

→ 重编

+ preserve_objects

最少 2 轮

CASE · ELEPHANT + SQUIRREL SEESAW

1.0

reasoning_type = causal · VQA checklist 4 / 4 passed · final score 1.0

ReasonGenPilot · Course Defense 2026

06/16

### Speaker Notes

成员二负责 edit 通路。Reason Agent 把指令分到四类：physical、temporal、causal、story，与 Reason50K 论文对齐。pipeline 五步：Reason、finalize_edit_prompt 注入物理约束、Qwen-Image 无 mask 指令编辑、K 候选 VQA 选优、不达标 refine 重编。大象松鼠跷跷板这个 case，reasoning_type 自动判定为 causal，VQA checklist 四比四全过。

## Slide 7

Member 3 — hybrid Route (1 / 2)

FULL RE-PAINT

07/16

Reason 重写场景 · T2I 从零重绘 · 原图仅用于推理

When the change is too large to keep pixels — recompose the scene from scratch.

edit

hybrid

DIMENSION

image + edit_prompt

scene_prompt

MECHANISM

→ Edit API

→ T2I (无 image 输入)

机制

PIXELS

Yes

No

保留原像素？

尽可能保留 — 局部改

从零重绘 — 像素不保留

edit_prompt

scene_prompt

REASON OUTPUT

Reason 输出

+ target_objects

整段场景描述

冰块融化 · 跷跷板倾斜

一束花分两束 · 白天 → 深夜

TYPICAL CASE

典型场景

单对象局部变化

数量 / 全局 / 季节 / 重组

Original image is used ONLY for reasoning. T2I never sees it — only the Reason Agent does.

ReasonGenPilot · Course Defense 2026

07/16

### Speaker Notes

成员三的 hybrid 是我们的工程亮点之一。它不在原图上改，Reason 看完原图后重写 scene_prompt，T2I 完全从零画。所以原图只用于推理，T2I 永远看不到。适用场景是 edit 做不了的整图变换，例如一束花分两束、白天变深夜、季节切换。

## Slide 8

Seven-Layer Post-Processing (hybrid)

QUALITY SAFEGUARDS

08/16

scene_prompt 进 T2I 前 — 7 个函数依序执行

In reason/hybrid_pipeline.py — fragment detection · anchor injection · geometry repair.

1

2

3

4

FRAGMENT

COMPLETENESS

PERSPECTIVE

STYLE

碎片修复

元素完整性

视角锚点

风格锚点

_validate_and_fix_

_ensure_instruction_

_inject_perspective_

_inject_style_anchor

scene_prompt

elements

anchor

从 visual_cues 重

重新注入指令命

锁定视点 + 空间

注入风格线索；

建碎片 prompt

名但 Reason 遗漏的对象

排布关系

变形场景自动跳过冲突

5

6

7

IDENTITY · PERSON AGE

LIGHTING

GEOMETRY

人物年龄锁定

光照行为

房间几何

_inject_person_age

_inject_light_behavior

_inject_room_geometry

锁定年龄段，防止随机换

光向 + 硬度 + 色温

窗位 / 地砖 / 结构锚点

为孩子 / 老人

一致性

FRAGMENT → T2I-READY

UNIT TESTS

7 sequential gates

36 / 36 passing · reason/test_hybrid_pipeline.py · runtime < 0.05 s

ReasonGenPilot · Course Defense 2026

08/16

### Speaker Notes

hybrid 真正的工程价值在这七层 scene_prompt 后处理。Reason Agent 给出的描述常常是一堆碎片，我们用七个函数依序打磨：碎片修复、元素完整性、视角锚点、风格锚点、人物年龄、光照行为、房间几何。它把 prompt 从碎句变成 T2I 能正确读懂的完整描述。这套机制有三十六个单元测试，全部通过。

## Slide 9

Member 4 — Router

MLLM-ONLY ROUTER

09/16

MLLM 自动分三通路

Fail loud, not silent. No keyword fallback — MLLM unconfigured = explicit RuntimeError.

DECISION FLOW (reason/router.py)

Prompt is required (all modes)

LEVEL 0

CORE QUESTION

prompt

空 prompt → ValueError: router: prompt is required...

改完之后还需要

原图像素吗？

Mode validity

LEVEL 1

mode

必须 ∈ {auto, gen, edit, hybrid}，否则 ValueError

Yes → preserve via edit

No → re-paint via hybrid

Manual override — input compatibility

LEVEL 2

manual

edit / hybrid 须传可读的 image，否则 ValueError

TWO ERROR CLASSES

auto + 结构规则 (zero cost)

LEVEL 3

ValueError

用户输入错误（请修改调用）

struct

无图直接 → gen，不调 MLLM

RuntimeError

auto + MLLM 分类器

LEVEL 4

环境 / MLLM 故障（请修部署）

MLLM

prompts/router_system.txt → 严格 JSON · 失败 → RuntimeError

所有信息 router: 前缀

23 / 23

UNIT TESTS · reason/test_router.py

6 类全部 mock： PromptRequired · ModeValidity · ModeInputCompatibility · AutoStructural · MLLMPath · MLLMErrors

ReasonGenPilot · Course Defense 2026

09/16

### Speaker Notes

成员四负责 Router 加集成。这里要特别澄清一个变化：Router 现在是 MLLM-only 设计，没有关键词降级。决策分四层：先校验 prompt 必填，再校验 mode 合法，再校验 manual override 的输入，无图直接 gen，剩下的统一交给 MLLM 分类器。错误只有两类，ValueError 是用户输入问题，RuntimeError 是 MLLM 或环境问题，所有消息以 router 冒号开头。六组单测共二十三个全部通过。

## Slide 10

Member 4 — Integration & Demo

CLI · WEB UI

10/16

一条命令 · 一个 Demo · 单一 --prompt 字段

CLI & Gradio share router.route() and the same dataclass schema — parity by construction.

pipeline.py

demo_gradio.py

UNIFIED CLI

GRADIO WEB UI

UI FIELDS

# auto — router decides; final JSON to stdout

Route 下拉 — auto / gen / edit / hybrid

$ python pipeline.py \

--image x.png \

--prompt "如果冰块融化会怎样" \

Prompt + 图像上传 · 无图直接走 gen

--mode auto \

--real-api

“Use real API” 开关 · 关 = dry-run

# 单一 --prompt：gen 当描述 / edit·hybrid 当反事实指令

UI OUTPUTS

DISPATCH

· before / after 图像并排

router.route() → run_gen / run_edit / run_hybrid

· 推理链 + VQA 面板

RETURN (shared dataclass)

· 完整 JSON viewer

final_image · route · reasoning_chain

vqa_result · metadata

→ http://127.0.0.1:7860

HTTP 200 OK

不带 --real-api 即 dry-run — SVG 占位图，无 API 消耗

CLI & Web UI 共享同一 router.route() 与同一 dataclass schema —— parity by construction

ReasonGenPilot · Course Defense 2026

10/16

### Speaker Notes

统一入口和 Demo。左边 pipeline 点 py 是 CLI，关键改动是单一 dash dash prompt 字段对所有 mode 通用，加 dash dash real-api 跑真实接口，不加就是 dry-run。右边 demo_gradio 点 py 是 Web UI，提供 Route 下拉、Prompt + 图像上传、real-api 开关，输出 before after 对比、推理链、VQA 面板和完整 JSON。两个表面共用同一个 router 和同一个 dataclass schema，行为完全一致。

## Slide 11

Text → Image · Poppy + Daisy + Windmill

SHOWCASE · gen

11/16

gen 通路实例 — prompt 优化两轮收敛

Real API · DashScope Qwen-Image · VQA 0.8 → 1.0 → 1.0

INPUT · PROMPT

OUTPUT · FINAL IMAGE (iter 2)

USER PROMPT

“A grass field of red poppies and yellow daisies

beside a wooden windmill at golden hour.”

BASELINE (iter 0)

2 ITER

＜final image＞

data/output/gen/poppy/final.png

＜baseline image＞

data/output/gen/poppy/baseline.png

VQA 0.8 · baseline missed “yellow daisies”

VQA score 1.0 / 1.0 · all constraints satisfied

ReasonGenPilot · Course Defense 2026 · Showcase 1 / 3

11/16

### Speaker Notes

接下来三页是真实 case 展示。第一个是 gen 通路：用户给一段描述性 prompt——草地上的红色罂粟、黄色雏菊和木风车。baseline 第一张图漏掉了黄色雏菊，VQA 打分零点八。Pipeline 自动拆出未满足的约束，生成 K 个候选 prompt，挑出最优的，第二轮直接到一点零，第三轮稳定在一点零。这就是 GenPilot 风格的优化循环。

## Slide 12

Local Edit · Elephant + Squirrel Seesaw

SHOWCASE · edit

12/16

edit 通路实例 — 局部编辑保留原像素

Real API · Qwen-Image instruction edit · reasoning_type = causal · VQA 4 / 4

INPUT · IMAGE + INSTRUCTION

OUTPUT · EDITED IMAGE

COUNTERFACTUAL INSTRUCTION

“如果大象和松鼠在草地上玩跷跷板会怎样？”

REFERENCE IMAGE

REASON

＜edited image＞

data/output/edit/seesaw/final.png

+ EDIT

＜original photo＞

data/input/edit/elephant_squirrel_grass.png

elephant + squirrel + grass field

checklist 4 / 4 passed · pixels of grass & sky preserved

ReasonGenPilot · Course Defense 2026 · Showcase 2 / 3

12/16

### Speaker Notes

第二个是 edit 通路：原图是大象松鼠在草地上，反事实指令问如果它们玩跷跷板会怎样。Reason Agent 推理出这是 causal 类，写出 edit_prompt 加保留对象列表，Qwen-Image 在原图上做无 mask 的指令编辑。VQA checklist 四比四全过，草地和天空的像素被很好地保留，只有跷跷板和大象松鼠的姿态局部改动。

## Slide 13

Full Re-paint · Day → Night Room

SHOWCASE · hybrid

13/16

hybrid 通路实例 — 整图重绘 · 原图仅供推理

auto → hybrid · reasoning_type = temporal · scene_prompt 经 7 层后处理 · VQA 1.0

INPUT · IMAGE + INSTRUCTION

OUTPUT · REPAINTED IMAGE

COUNTERFACTUAL INSTRUCTION

“如果这个房间在深夜会是什么样子？”

REFERENCE IMAGE (used only by Reason)

REASON

＜nighttime room＞

data/output/hybrid/room_night/final.png

+ 7-LAYER

+ T2I

＜daytime room photo＞

data/input/hybrid/room_day.png

window / sofa / floor tiles · daylight

scene_prompt 2571 chars · VQA 1.0

ReasonGenPilot · Course Defense 2026 · Showcase 3 / 3

13/16

### Speaker Notes

第三个是 hybrid 通路，也是最能体现工程价值的一个。原图是一个白天的房间，指令问如果是深夜会是什么样。Router 自动判定 hybrid，Reason Agent 推断 reasoning_type 是 temporal。注意 T2I 完全不看原图，只看 Reason 重写的 scene_prompt。这段 prompt 经过七层后处理后达到两千五百七十一字符，覆盖光照、几何、家具位置、深夜氛围。最终 VQA 一点零。

## Slide 14

Conclusion

BY THE NUMBERS

14/16

A training-free agent — shipped in four quadrants.

三条互补通路 + 一个统一入口 + 真实 API 全 1.0 + MLLM-only Router 严格错误分类。

ROUTES

ENTRY

UNIT TESTS

ROUTER

3

1

59

MLLM

-only

complementary routes

unified CLI + Web UI

passing · 0.05 s runtime

explicit failure

gen · edit · hybrid

36 hybrid + 23 router

无关键词降级 · 显式错误

pipeline.py · demo_gradio.py

TAKEAWAY

Zero training. Three routes. One CLI.

纯靠 MLLM Agent 编排 + 公开 API — 同时解决描述失配和反事实编辑两类痛点，

不微调一个权重，不自建一份数据。

ReasonGenPilot · Course Defense 2026

14/16

### Speaker Notes

数字总结一下。三条互补通路，一个统一入口加一个 Web Demo，五十九个单元测试零点零五秒全过，Router 是 MLLM-only 设计严格抛错。一句话收尾：零训练、三通路、一条命令；纯靠 MLLM Agent 编排加公开 API，同时覆盖描述失配和反事实编辑两类痛点，不微调一个权重，不自建一份数据。

## Slide 15

Team & Division of Labor

TEAM ROSTER

15/16

四人串行交付 — base → edit → hybrid → integration

Each member's deliverables become the next member's input.

梁爽

陈颖妍

01

02

MEMBER 1 · FOUNDATION + gen

MEMBER 2 · edit PIPELINE

DELIVERABLES

DELIVERABLES

api_client · t2i_client · schemas

Reason Agent · edit_client · edit_pipeline

gen_pipeline + GenPilot optimization loop

Qwen-Image instruction edit + VQA refine

Poppy / Daisy / Windmill · VQA 0.8 → 1.0 → 1.0

Seesaw · reasoning_type = causal · VQA 4 / 4

苏香广

艾博显

03

04

MEMBER 3 · hybrid PIPELINE

MEMBER 4 · ROUTER + INTEGRATION

DELIVERABLES

DELIVERABLES

hybrid_pipeline + 7-layer scene_prompt fix

MLLM-only router · pipeline.py · Gradio demo

36 unit tests · comparison experiment script

23 unit tests · defense doc + this deck

Split-bouquet · Day → night room · VQA 1.0

4-route auto-dispatch · 全部 23 / 23 router tests 通过

梁爽 → 陈颖妍 → 苏香广 → 艾博显 · base, then edit, then hybrid, then integration

HAND-OFF

ReasonGenPilot · Course Defense 2026

15/16

### Speaker Notes

最后看团队分工。我们四个人按基座、edit、hybrid、集成的顺序串行交付，每位的产出是下一位的输入。梁爽搭基座加 gen 通路；陈颖妍在地基之上做 edit 通路加 VQA refine 闭环；苏香广完成 hybrid 通路并实现七层后处理与三十六个单测；艾博显负责 Router、统一入口、Gradio Demo、二十三个 Router 单测、答辩文档与本套幻灯。

## Slide 16

REASONGENPILOT · END

16/16

Thank you.

Q & A

DELIVERABLES

VQA 1.0 across the board

3 pipelines · 1 unified entry · 59 unit tests · 23 router tests · MLLM-only routing

Branch testWork3 · Course Defense 2026 · Team of 4

梁爽 · 陈颖妍 · 苏香广 · 艾博显

### Speaker Notes

讲完了，谢谢老师同学聆听，欢迎提问。
